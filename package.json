{
  "name": "amath-koth-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A-Math King of the Hill tournament manager for Cloudflare Workers + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create amath-koth-db",
    "db:migrate:local": "wrangler d1 migrations apply amath-koth-db --local",
    "db:migrate": "wrangler d1 migrations apply amath-koth-db --remote",
    "test": "node --test tests/core.test.mjs tests/documents.test.mjs",
    "check": "node --check src/core.js && node --check src/documents.js && node --check src/worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "pdf-lib": "^1.17.1"
  }
}
